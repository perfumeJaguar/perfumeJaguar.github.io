/**
 * DODREI — CONFIG SCHEMA v1
 * Metadata only. Runtime values live in ../config.js.
 *
 * The Control page renders every value it finds in the current config.
 * This schema adds meaning: labels, bounds, select options, collection identity,
 * compatibility rules, and comments used when exporting a canonical config.js.
 */
window.DODREI_CONFIG_SCHEMA = {
  project: "DODREI",
  schemaVersion: 1,

  groups: [
    { id: "meta", label: "META", description: "Config identity and compatibility metadata." },
    { id: "app", label: "APP", description: "Global timing, fullscreen and runtime settings." },
    { id: "render", label: "RENDER", description: "Internal resolution and analysis cost." },
    { id: "media", label: "MEDIA", description: "Archive discovery, image sets and rolling decoded pool." },
    { id: "interaction", label: "INTERACTION", description: "Pointer / touch normalization." },
    { id: "audio", label: "AUDIO", description: "Playback, analysis and interactive effects." },
    { id: "visual", label: "VISUAL", description: "Crop, feedback, rupture, mode playlist and stage pipeline." },
    { id: "telemetry", label: "TELEMETRY", description: "Foreground terminal-like information layer." },
    { id: "control", label: "CONTROL", description: "Editor behavior; ignored by the artwork runtime." },
  ],

  // Exact-path metadata. Missing entries are still editable through inferred types.
  fields: {
    "meta.project": { type: "string", readOnly: true, description: "Project identity used during import compatibility checks." },
    "meta.schemaVersion": { type: "integer", readOnly: true, min: 1, description: "Shape/version of the configuration contract." },
    "meta.configRevision": { type: "integer", min: 1, step: 1, description: "Human-controlled revision of config values." },
    "meta.generatedBy": { type: "string", description: "Origin marker for the current file." },

    "app.title": { type: "string", description: "Visible artwork title." },
    "app.version": { type: "string", description: "Artwork/runtime version; independent from schemaVersion." },
    "app.targetFps": { type: "integer", min: 15, max: 120, step: 1, unit: "fps" },
    "app.requestFullscreenOnStart": { type: "boolean" },
    "app.preventContextMenu": { type: "boolean" },
    "app.modeDurationSec": { type: "number", min: 0.25, max: 600, step: 0.25, unit: "s", description: "Time before advancing to the next enabled visual mode." },
    "app.imageSwitchSec": { type: "number", min: 0.01, max: 10, step: 0.01, unit: "s" },

    "render.pixelDensity": { type: "number", min: 0.5, max: 3, step: 0.25 },
    "render.background": { type: "integer", min: 0, max: 255, step: 1 },
    "render.maxBufferLongEdgeMobile": { type: "integer", min: 240, max: 4096, step: 10, unit: "px" },
    "render.maxBufferLongEdgeDesktop": { type: "integer", min: 240, max: 4096, step: 10, unit: "px" },
    "render.analysisWidthMobile": { type: "integer", min: 32, max: 1024, step: 1, unit: "px" },
    "render.analysisWidthDesktop": { type: "integer", min: 32, max: 1024, step: 1, unit: "px" },
    "render.analysisEveryNFrames": { type: "integer", min: 1, max: 60, step: 1 },
    "render.analysisPixelStep": { type: "integer", min: 1, max: 16, step: 1 },

    "media.videosEnabled": { type: "boolean" },
    "media.autoDiscoverImages": { type: "boolean" },
    "media.githubOwner": { type: "string" },
    "media.githubRepo": { type: "string" },
    "media.githubBranch": { type: "string" },
    "media.githubImageDir": { type: "string" },
    "media.activeImageLimit": { type: "integer", min: 1, max: 500, step: 1 },
    "media.initialLoadConcurrency": { type: "integer", min: 1, max: 16, step: 1 },
    "media.rotationBatchSize": { type: "integer", min: 1, max: 100, step: 1 },
    "media.rotationIntervalSec": { type: "number", min: 0.1, max: 3600, step: 0.1, unit: "s" },
    "media.rotationLoadConcurrency": { type: "integer", min: 1, max: 16, step: 1 },
    "media.rotationPolicy": { type: "select", options: ["shuffle-bag"], description: "Candidate selection policy. New policies can be registered later without changing this field." },

    "interaction.smoothing": { type: "number", min: 0, max: 1, step: 0.01 },
    "interaction.pressBoost": { type: "number", min: 0, max: 5, step: 0.01 },

    "audio.enabled": { type: "boolean" },
    "audio.masterVolume": { type: "number", min: 0, max: 1, step: 0.01 },
    "audio.directNativeOutput": { type: "boolean" },
    "audio.pcmWindowSize": { type: "integer", min: 32, max: 8192, step: 32 },
    "audio.waveformPoints": { type: "integer", min: 16, max: 2048, step: 1 },
    "audio.minRate": { type: "number", min: 0.25, max: 4, step: 0.001 },
    "audio.maxRate": { type: "number", min: 0.25, max: 4, step: 0.001 },
    "audio.lumaRateMin": { type: "number", min: 0.25, max: 4, step: 0.001 },
    "audio.lumaRateMax": { type: "number", min: 0.25, max: 4, step: 0.001 },
    "audio.pressRateBoost": { type: "number", min: -1, max: 1, step: 0.001 },
    "audio.fxEnabled": { type: "boolean" },
    "audio.fxWetMin": { type: "number", min: 0, max: 1, step: 0.001 },
    "audio.fxWetMax": { type: "number", min: 0, max: 1, step: 0.001 },
    "audio.minFilterHz": { type: "number", min: 20, max: 24000, step: 1, unit: "Hz" },
    "audio.maxFilterHz": { type: "number", min: 20, max: 24000, step: 1, unit: "Hz" },
    "audio.maxDelayTime": { type: "number", min: 0, max: 5, step: 0.01, unit: "s" },
    "audio.maxDelayFeedback": { type: "number", min: 0, max: 0.99, step: 0.01 },
    "audio.maxDistortion": { type: "number", min: 0, max: 1, step: 0.01 },

    "visual.enabled": { type: "boolean" },
    "visual.sourceCropMinZoom": { type: "number", min: 1, max: 8, step: 0.01 },
    "visual.sourceCropMaxZoom": { type: "number", min: 1, max: 8, step: 0.01 },
    "visual.sourceCropTouchBoost": { type: "number", min: 0, max: 4, step: 0.01 },
    "visual.sourceCropPanFactor": { type: "number", min: 0, max: 2, step: 0.01, description: "Legacy crop-pan coefficient retained for compatibility; v0.8 overflow crop placement does not currently use it directly." },
    "visual.sourceCropOverflowPan": { type: "number", min: 0, max: 1, step: 0.01 },
    "visual.touchTransitionSlowdown": { type: "number", min: 0, max: 5, step: 0.01 },
    "visual.feedbackScale": { type: "number", min: 0.8, max: 1.2, step: 0.001 },
    "visual.feedbackAlpha": { type: "integer", min: 0, max: 255, step: 1 },
    "visual.feedbackResolutionScaleMobile": { type: "number", min: 0.1, max: 1, step: 0.01 },
    "visual.feedbackResolutionScaleDesktop": { type: "number", min: 0.1, max: 1, step: 0.01 },
    "visual.mosaicColsMobile": { type: "integer", min: 2, max: 256, step: 1 },
    "visual.mosaicColsDesktop": { type: "integer", min: 2, max: 256, step: 1 },
    "visual.photoCutMs": { type: "integer", min: 16, max: 5000, step: 1, unit: "ms" },
    "visual.rgbTearMaxPx": { type: "number", min: 0, max: 1000, step: 1, unit: "px" },
    "visual.vignetteStrength": { type: "number", min: 0, max: 2, step: 0.01 },
    "visual.crushContrast": { type: "number", min: 0.1, max: 10, step: 0.01 },
    "visual.crushPosterizeLevels": { type: "integer", min: 2, max: 255, step: 1 },
    "visual.crushIntruderAlpha": { type: "integer", min: 0, max: 255, step: 1 },
    "visual.touchRuptureContrast": { type: "number", min: 0.1, max: 10, step: 0.1 },
    "visual.touchRuptureBands": { type: "integer", min: 1, max: 128, step: 1 },
    "visual.touchRuptureResolutionScaleMobile": { type: "number", min: 0.1, max: 1, step: 0.01 },
    "visual.touchRuptureResolutionScaleDesktop": { type: "number", min: 0.1, max: 1, step: 0.01 },
    "visual.touchRuptureFrameSkipMobile": { type: "integer", min: 1, max: 30, step: 1 },
    "visual.touchRuptureFrameSkipDesktop": { type: "integer", min: 1, max: 30, step: 1 },
    "visual.swipeFeedbackThreshold": { type: "number", min: 0, max: 5, step: 0.01 },
    "visual.swipeFeedbackScaleMin": { type: "number", min: 0.8, max: 1.2, step: 0.001 },
    "visual.swipeFeedbackScaleMax": { type: "number", min: 0.8, max: 1.2, step: 0.001 },
    "visual.swipeFeedbackAlphaMin": { type: "integer", min: 0, max: 255, step: 1 },
    "visual.swipeFeedbackAlphaMax": { type: "integer", min: 0, max: 255, step: 1 },
    "visual.modeControl.strategy": { type: "select", options: ["sequence", "shuffle"] },
    "visual.modeControl.startIndex": { type: "integer", min: 0, max: 999, step: 1 },
    "visual.modeControl.loop": { type: "boolean", description: "Sequence strategy only: false holds on the last enabled mode." },

    "telemetry.enabled": { type: "boolean" },
    "telemetry.author": { type: "string" },
    "telemetry.maxEvents": { type: "integer", min: 1, max: 200, step: 1 },
    "telemetry.opacity": { type: "number", min: 0, max: 1, step: 0.01 },
    "telemetry.secondaryOpacity": { type: "number", min: 0, max: 1, step: 0.01 },
    "telemetry.faintOpacity": { type: "number", min: 0, max: 1, step: 0.01 },
    "telemetry.fontSizeMobile": { type: "number", min: 4, max: 64, step: 0.5, unit: "px" },
    "telemetry.fontSizeDesktop": { type: "number", min: 4, max: 64, step: 0.5, unit: "px" },
    "telemetry.lineHeight": { type: "number", min: 0.5, max: 4, step: 0.01 },
    "telemetry.marginMobile": { type: "number", min: 0, max: 200, step: 1, unit: "px" },
    "telemetry.marginDesktop": { type: "number", min: 0, max: 200, step: 1, unit: "px" },
    "telemetry.glitchOnMotion": { type: "boolean" },
    "telemetry.glitchLabels": { type: "boolean" },

    "control.localDraftKey": { type: "string", description: "localStorage key used by the Control page." },
    "control.importPolicy": { type: "select", options: ["compatible-merge"], readOnly: true },
  },

  collections: {
    "media.imageExtensions": { kind: "primitive-list", itemType: "string", label: "IMAGE EXTENSIONS" },
    "media.imageSets": { kind: "id-list", idKey: "id", label: "IMAGE SETS", reorderable: true, addable: true, allowUnknownItems: true },
    "visual.touchPalette.thresholds": { kind: "primitive-list", itemType: "number", expectedLength: 3, label: "PALETTE THRESHOLDS" },
    "visual.touchPalette.colors": { kind: "json", expectedLength: 4, label: "PALETTE COLORS" },
    "visual.presets": { kind: "id-list", idKey: "id", label: "MODE PLAYLIST", reorderable: true, toggleKey: "enabled", allowUnknownItems: false },
    "visual.pipeline": { kind: "id-list", idKey: "id", label: "VISUAL PIPELINE", reorderable: false, toggleKey: "enabled", allowUnknownItems: false },
  },

  // Reserved compatibility layer. Add aliases when a path is renamed.
  // Example: "visual.oldName": "visual.newName"
  aliases: {},
};
